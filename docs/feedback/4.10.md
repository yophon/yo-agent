# Phase 4.10 真机复验记录(2026-07-03)

环境:gpt-5.5(OPENAI_BASE_URL 代理,chat completions 路径)· supervised 档 · TUI(tmux 驱动)+ jsonl 模式。

## ✅ 已验证

- **熔断误伤已修(4.10a)**:两种模式共 8+ 次同参 `subagent_spawn`(含「一次发 3 个同参」的
  jsonl 批次),全程零「检测到死循环」。对照 feedback/4.9 实录:旧版第 3 次同参 spawn 即熔断。
- **/tasks 面板全链路(4.10c)**:`/tasks` 打开列表(✓ 完成态、耗时 4s/3s、摘要 hi)→ Enter
  进入子代理事件流详情(读到子会话 EventLog:`💬 hi`、`— 轮结束(end_turn)`)→ Esc 返回列表
  → Esc 关闭。运行中打开面板亦正常。
- **批内审批链(4.10b 准入段)**:同批多 spawn 首个弹审批面板,「总是允许」后其余走会话缓存
  自动放行,与设计一致。

## ⚠️ 未决:真机并行批次未能触发(模型端行为,非内核)

- 多轮明确指示(中文/英文、点名 tool_calls 数组一次发 3 个),gpt-5.5(经当前代理)**每个
  assistant 响应始终只发 1 个 tool_call**,采样窗口内同屏「运行中」子代理最多 1 个——与
  feedback/4.9 模型自述「刚才两次确实是我直接连续调用了」同因。
- 排查过我们侧:`openai.ts` 请求体**未禁** `parallel_tool_calls`(OpenAI 默认开);provider
  对多 tool_calls 增量解析正常(索引数组 → 逐个 ToolCallStart)。内核侧批内并发有单测兜底
  (真并发计时断言,`tool-concurrency.test.ts`)。
- 候选跟进(下阶段立项池):
  1. `subagent_spawn` 工具描述加提示「可在同一响应中并行发起多个调用」(便宜的模型侧 nudge);
  2. 换 Anthropic key 真机复验(Claude 系模型常发并行 tool_use 批);
  3. 查代理网关是否剥离/改写 parallel tool calls 行为。
