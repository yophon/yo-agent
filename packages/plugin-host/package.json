{
  "name": "@yo-agent/plugin-host",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PluginHost（4E / ADR-18）：第三方插件（不可信代码）跑在独立 Worker，经结构化 IPC + 心跳重连与主进程通信；崩溃不拖垮主进程、读不到主进程 secret、工具走主审批流。独立成包隔离 Worker/IPC 依赖（与 surface-acp 独立成包同理）。",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "dependencies": {
    "@yo-agent/protocol": "workspace:*",
    "@yo-agent/tools": "workspace:*",
    "@yo-agent/kernel": "workspace:*"
  }
}
