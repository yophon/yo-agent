{
  "name": "@yo-agent/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "yo-agent 协议单一事实源：AgentEvent sealed union + JSON-RPC 方法表 + cursor/resume 语义。TS 类型 + 运行时校验 + 可导出 JSON Schema（给 Go bridge）。",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "zod-to-json-schema": "^3.24.1"
  }
}
